# Лабораторная работа 3
## Kubernetes

Kubernetes — система оркестровки контейнеров с открытым исходным кодом, готовая для production и предназначенная для автоматизации размещения, масштабирования и управления контейнерами.

Для начала работы в с кластером кубернетеса и его изучения, его надо развернуть локально, самым простым способом для этого является использование minikube. Вся работа будет продемонстрирована на MacOS, альтернативные команды для остальных ОС можно найти на оффициальном сайте minikube: https://kubernetes.io/ru/docs/tasks/tools/install-minikube/

Пройдем шаги установки minikube и всех необходимых для работы утилит по порядку:
1. Необходимо установить kubectl - командную утилиту для управления кластерами кубернетеса, позволяющая просматривать и редактировать ресурсы и конфиги множества кластеров:
```brew install kubectl ```
2. Затем проверим, что kubectl успешно установился:
```kubectl version --client```
3. После установки kubectl, установим сам minikube, который развернет нам локальный кластер с единственной нодой:
```brew install minikube```
4. Так же проверим, что minikube успешно установился:
```minikube status```
5. Запустим minikube
```minikube start```
Готово, все необходимое установлено, и теперь можно переходить к конфигурацию и запуска нашего локального кластера.
> Скриншотов по процессу установки к сожалению не будет, так как kubectl и minikube были установлены у меня уже очень давно

Так же учтите, что minikube требует некое минимальное количество аппаратных ресурсов, так у меня докеру было выделено всего 3 CPU, а minikube надо 4, и при запуске minikube выдал мне следующую ошибку при запуске:
```
😄  minikube v1.32.0 на Darwin 14.6.1 (arm64)
✨  Используется драйвер docker на основе существующего профиля
- Ensure your docker daemon has access to enough CPU/memory resources.
- Docs https://docs.docker.com/docker-for-mac/#resources

⛔  Exiting due to RSRC_INSUFFICIENT_CORES: Requested cpu count 4 is greater than the available cpus of 3
```
В данной ситуации просто измените доступные ресурсы для докера, перезапустите его, и попробуйте запустить minikube заново.

## Minikube
Перейдем к запуску кластера, в котором будем поднимать поды, деплойменты, сервисы и остальные компоненты кластера. Но предварительно хотелось бы ввести некоторую пометку, если вы собираетесь использовать ваши личные, находящиеся локальные образы докера, просто так это сделать не получиться, для этого надо запускать minikube следующим способом:
1. Запустим minikube в окружении докера
```eval $(minikube -p minikube docker-env)```
2. Сбилдим наши образы находясь в контексте командной строки в которой ввели преыдущую комманду
3. Во всех конфиг файлах, которые управляют подами, выставим для них
```imagePullPolicy: Never```
4. Готово, можно применять конфигурационные файлы ресурсов, использующих ваши локальные образы докера

Переходя к запуску, введем команду
```minikube start```
И получим нечто подобное
```
😄  minikube v1.32.0 на Darwin 14.6.1 (arm64)
✨  Используется драйвер docker на основе существующего профиля
👍  Запускается control plane узел minikube в кластере minikube
🚜  Скачивается базовый образ ...
🔄  Перезагружается существующий docker container для "minikube" ...
🐳  Подготавливается Kubernetes v1.28.3 на Docker 24.0.7 ...
🔗  Configuring bridge CNI (Container Networking Interface) ...
🔎  Компоненты Kubernetes проверяются ...
💡  After the addon is enabled, please run "minikube tunnel" and your ingress resources would be available at "127.0.0.1"
    ▪ Используется образ registry.k8s.io/ingress-nginx/controller:v1.9.4
    ▪ Используется образ gcr.io/k8s-minikube/storage-provisioner:v5
    ▪ Используется образ registry.k8s.io/ingress-nginx/kube-webhook-certgen:v20231011-8b53cabe0
    ▪ Используется образ registry.k8s.io/ingress-nginx/kube-webhook-certgen:v20231011-8b53cabe0
🔎  Verifying ingress addon...
🌟  Включенные дополнения: storage-provisioner, ingress, default-storageclass
🏄  Готово! kubectl настроен для использования кластера "minikube" и "default" пространства имён по умолчанию
```
У вас может не быть следующих строк, или они могут быть другими, так и должно быть, это плагины которые я установил дополнительно к minikube, один из них - ingress
```
💡  After the addon is enabled, please run "minikube tunnel" and your ingress resources would be available at "127.0.0.1"
    ▪ Используется образ registry.k8s.io/ingress-nginx/controller:v1.9.4
    ▪ Используется образ gcr.io/k8s-minikube/storage-provisioner:v5
    ▪ Используется образ registry.k8s.io/ingress-nginx/kube-webhook-certgen:v20231011-8b53cabe0
    ▪ Используется образ registry.k8s.io/ingress-nginx/kube-webhook-certgen:v20231011-8b53cabe0
🔎  Verifying ingress addon...
🌟  Включенные дополнения: storage-provisioner, ingress, default-storageclass
```
Супер, наш кластер поднят, теперь надо это проверить, и по необходимости (если у вы до этого уже работали с kubectl) переключится на его контекст, проверим и переключимся на него следующими командами:
```kubectl config get-contexts``` - Выведет доступные кластеры, к которым подключен ваш kubectl, среди них должен быть ```minikube```
```kubectl config use-context minikube``` - Переключит вас в контекст minikube кластера, и выведет ```Switched to context "minikube".``` в случае успеха.

## Проект
Проект который будем разворачивать - обычный сервер, имеющий один эндпоинт ```/weatherforecast```, который возвращает рандомный прогноз погоды.

## K8s
Для начала определим, что kubernetes использует в качестве самой базовой единицы - контейнеры докера (хотя не обязательно докера, но это уже другая история). А значит нам необходимо создать докерфайл для нашего проекта, что бы затем разворачивать его в кластере. Докерфайл будет такой же простой как и сам проект, мы просто загрузим необходимые sdk и рантайм, скопируем все файлы проекта, сбилдим его, и запустим, ничего сложного.
```
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build-env
WORKDIR /app
COPY ../Clouds ./Clouds
RUN dotnet publish Clouds -c Release -o out

FROM mcr.microsoft.com/dotnet/aspnet:8.0
WORKDIR /app
COPY --from=build-env /app/out .
ENTRYPOINT ["dotnet", "Clouds.dll"]
```
Когда наш докерфайл готов, мы можем приступить к созданию конфигурационных файлов для развертывания нашего приложения в kubernetes. Все контейнеры которые вы будете запускать в кластере будут располагаться в базовых едицинах kubernetes - подах. В поде может находиться несколько контейнеров, но у нас будет лишь один контейнер в поде. Но для доступа извне кластера к нашему приложению одних подов будет недостаточно, для этого понядобятся сервисы - они занимаются исходящего транспортировкой трафика до соответствующих им подов, все это описывается в конфигурационных файлах.
Стоит понимать, что просто подами управлять не так уж и приятно, если какой-то под завалится - придется вручную создавать новый, а о плавной раскатке новой версии вручную даже думать не хочется. Для этого в kubernetes существуют такие ресурсы как StatefulSet, ReplicaSet, DaemonSet и Deployment. Все они управляют подами по разным правилам, и о них можно много чего написать, но в данном случае разберем только Deployment. С помощью него можно настроить сколько реплик подов должно поддерживаться на постоянной основе, при падении пода, он сразу же создаст новый, что бы возместить упавший под. Если изменить в его конфиге кол-во подов, он создаст/удалит нужное кол-ов подов, для достижения целевого количества. Так же с помощью него можно настроить политику изменения количества подов и раскатки новой версии. Можно задать плавное обновление, которое позволит, при обновлении версии образа, обновлять все поды постепенно, то есть он создаст новый под, и только когда удостоверится что он успешно работает (например с помощью health-check'a), удалит один из подов предыдущей версии.
А теперь перейдем к практике, ниже представлен конфиг deployment'а
![image](https://github.com/user-attachments/assets/50cb1bd9-55a8-4cad-a499-8b2b82f2fd56)
Тут можно увидеть много чего, но начнем с начала:
```apiVersion: apps/v1``` - задает версию api кубернетеса, откуда мы будем брать нужный нам ресурс
```kind: Deployment``` - задает тип ресурса Deployment - то, что нам и надо
```metadata``` - секция метаданных, здесь задется имя, пространство имен в котором создается наш ресурс, и лейблы, по которым в последствии можно будет ссылаться или искать данный ресурс
```spec``` - секция спецификации ресурса
```spec.replicas``` - количество реплик, то есть количество подов, которое должен поддерживать наш Deployment
Так же можно увидеть конструкции вида {{.Values.replicas}} - это не из кубернетеса, это из helm, с помощью данных конструкции можно превратить свой конфиг файл ресурса кубернетеса в шаблон, что бы в последствии с легкостю создавать различные конфигурации сервисов, и с легкостью их менять, без необходимости ручного изменения конфига. Пойдем дальше по файлу конфига:
```spec.strategy``` - стратегия управления подами, здесь мы задаем тип ```RollingUpdate``` с ```maxSurge: 1``` и ```maxUnavailable: 0```, это значит, что мы используем стратегию плавного обновления, и допускаем превышение кол-ва подов на 1, но не допускаем кол-во подов меньше чем наше целевое значение. А значит он будет создавать новый под, и только после этого удалять старый.
```spec.selector``` - на какие поды будет ориентироваться наш Deployment, что бы определить какое кол-во подов сейчас существует, и какими подами он вообще управляет для удаления
```spec.template``` - секция задания спецификации наших подов
```spec.template.metadata``` - мета данные подов
```spec.template.spec``` - спецификации подов, здесь задаются следующие параметры: на каких нодах должны находиться наши поды ```nodeSelector.nodeGroupPurpose```, какие контейнеры создавать в подах ```containers```
Разберем конфигурацию контейнера:
```resources``` - задает минимальные, и ожидаемые требования по ресурсам, если говорить по простому, минимальные ```requests``` - минимальные ожидания ресурсов контейнера, который ему должен предоставить kuberntes, а ```limits``` - максимально возможные ресурсы для потребления контейнера, если он привысит данные лимиты - kubernetes убьет его.
```image``` - используемый докер образ
```imagePullPolicy``` - политика загрузки и использования образа
```env``` - переменные окружения которые будут заданы в контейнере
```ports``` - конфигурация портов
```readinessProbe``` - задается политика определения готов ли под к работе, в данном случае мы используем ```httpGet``` то есть kubernetes будет пытаться стучаться в определенный эндпоинт, пока не получит успешный ответ, и только после этого контейнер будет считаться успешно запущенным, и когда все контейнеры готовы - тогда и под готов.

Наш deployment готов, мы можем его применить и наше приложение будет работать (просто так его применить не получиться, потому что в нем используются шаблоны для helm, если хотите применить его просто так, то замените все шаблоны на свои значения в файле). Сделать это можно с помощью ```kubectl apply -f deployment.yaml``` - эта команда, создаст или обновит существующий Deployment, который в свою очередь сразу же начнет управлять подами - создавать, удалять.

Но просто поды и deployment в контексте одного приложения не имеют смысла, так как они в кластере одни, и они не используются другими подами, а к ним нужен доступ извне. Тут нам на помощь приходят такие ресурсы как сервисы. Они конфигурируют как прослушивать внешний трафик, и куда его маршрутизировать и балансировать.
![image](https://github.com/user-attachments/assets/4c7e4ee1-82f6-4de8-a0a6-3130a0a913aa)
Как можно увидеть, здесь все намного проще, как может показаться на первый взгляд, но на самом деле это не так, так как существует огромное количество возможных конфигураций.
Давайте разберем его:
```apiVersion```, ```kind``` и ```metadata``` - То же самое что и в deployment
```spec.selector``` - селектор подов, на который сервис будет перенаправлять трафик
```spec.type``` - тип сервиса, может быть ```NodePort``` (сервис привязывается к порту выбранных нод в кластере и прослушивает их), ```ClusterIP``` - прослушивает определенный IP пренадлежащий кластеру, есть еще ```LoadBalancer``` и ```ExternalName``` но это уже сложнее и лучше почитать оффициальную документацию для лучшего понимания.
```spec.ports``` - правила маршрутизации портов, то есть какой порт мы будем прослушивать, и на какой порт в целевых подах будет прокидывать трафик
На этом собственно все, мы можем применить конфигурации нашеих деплоймента и сервиса, и получить готовое работающее, относительно отказоустойчивое приложение, с доступом извне кластера.
(тут скриншотов работоспособности не будет, так как я сразу написал helm чарты и конфиги с использованием шаблонов, поэтому все скришноты работы будут в следующем разделе)

## Helm
Helm - один из самых популярных пакетных менеджеров для Kubernetes. Данный инструмент предоставляет множество преимуществ, выделим ключевые:
1. Текстовая шаблонизация конфигураций ресурсов
2. Управление релизами - установка, обновление (апгрейд), откат и удаление
3. Отслеживание раската релиза, с возможностью автоматического отката в случае неудачи
4. Огромное сообщество, которое создало множество готовых helm-чартов для популярных инфраструктурных элементов, например для баз данных, брокеров сообщений и т.д. - то есть можно не вручную самим создавать и поднимать всю инфру, а загрузить готовые helm-чарты, создав тем самым всю инфру без усилий, а при необходимости подредактировать их.

Первое преимущество вы уже увидели выше, так что перейдем к раскатке нашего приложения в кластере при помощи helm.
Для начала необходимо создать два файла ```Chart.yaml``` и ```values.yaml``` - первый из них описывает наш чарт (то есть наше приложение), а второй описывает значения которые будут находиться и использоваться в шаблонизации наших конфигураций (и задает для них значения по умолчанию)
Сhart и values файлы выглядят следующим образом:
![image](https://github.com/user-attachments/assets/8a102acf-0ea7-484b-bb56-667cbcee2f4f)
![image](https://github.com/user-attachments/assets/ab6cb79b-78c7-42c4-b493-c7771427ff1c)

Все готово к деплою, приступим:
1. запустим наш ```minikube```: ```minikube start```
2. переведем докер в контекст миникуба: ```eval $(minikube -p minikube docker-env --shell=bash)```
3. создадим образ в данном контексте: ```docker build -t clouds:v1 -f ./Clouds/Dockerfile .```
4. пометим нашу ноду миникуба меткой: ```kubectl label nodes minikube nodeGroupPurpose=minikube```
5. обновим зависимости helm: ```helm dependency build```
6. произведем апгрейд релиза (или установим его если его нет):
   ```
   helm upgrade --install --set image=clouds:v1 \
             --set imagePullPolicy=Never \
             --set environment=Production \
             --set commit_hash=local \
             --set nodeGroupPurpose=minikube \
             --set reqCpuCores=0 \
             --set reqMemory=0 \
             --set limCpuCores=0 \
             --set limMemory=0 \
            clouds ./ --namespace default --create-namespace;
   ```
Последний шаг разберем подробнее, мы используем helm, что бы он обновил релиз на основе нашего чарта, при этому мы используем флаг ``--install`` - helm создаст релиз, если его нет.
Затем идут флаги ```--set``` - переопределение значений из values, для подстановки их в шаблон конфига.
Дальше указываем название релиза ```clouds```, директорию чарта ```./```, пространство имен для релиза (и создать его если не найден) ```--namespace default --create-namespace```
Готово!!! Наш проект развернут в кластере, и мы можем получить к нему доступ с помощью команды ```minikube service clouds```, проверим это.
![image](https://github.com/user-attachments/assets/cfa8fb0e-9fc0-418e-8d3f-b9b4ec496263)
Удостоверимся, что это действительно наш проект, и он и вправду запущен в нашем кластере:
![image](https://github.com/user-attachments/assets/3f21bfcc-9086-49a7-974b-1d0ff9f39d73)
Как можно увидеть, и вправду у нас запущено два пода, которыми управляет deployment, и на которые ссылается наш сервис.
А попробуем теперь обновить наше приложение, и сделать апгрейд с помощью helm'a. И так, добавим в наш сервис эндпоинт ```/newsuperpuperendpoint```, и затем повторим все комманды которые вводили в первый раз, но стоит заметить, наше приложение не будет обновляться просто так, если мы не изменим какие-то параметры шаблона пода, для этого можем добавить в лейблы подов следующую строку: ```date: "{{ .Release.Time.Seconds }}"``` и выставить ```imagePullPolicy: Always``` у нашего deployment'a, . (Важная пометка: так как у нас миникуб, и мы используем локальные образы, ```imagePullPolicy: Always``` нельзя :( - поэтому просто меняем в командах тэг образа). И так, запускаем наши команды с тегом для образа v2, и видим следующий вывод:
![image](https://github.com/user-attachments/assets/2446aa28-57b5-4314-bc4d-14d9687b5313)
Ура, наш релиз обновился, можем проверять наш новый эндпоинт.
![image](https://github.com/user-attachments/assets/2f7b770b-20f2-4646-8e5f-b05d709632d4)
Супер, все работает!

## Выводы
Kubernetes является очень мощным инструментов для менеджмента и расположения своих приложений с помощью контейнеров. Мы рассмотрели очень малую часть функциональности которую он предоставляет, поэтому очень советую ознакомится с ним полностью. Helm в свою очередь делает работу с kubernetes еще проще и приятнее, предоставляя готовые чарты, шаблонизацию конфигураций, управление релизами, и множество автоматизаций. Спасибо за внимание!









